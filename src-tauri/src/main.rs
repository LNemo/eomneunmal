fn main() {
    // The full Tauri runtime is wired in a later packaging pass. Keeping the
    // binary minimal lets the privacy/core library compile and test before any
    // native capture permission is requested.
    println!("없는말 desktop shell scaffold");
}
